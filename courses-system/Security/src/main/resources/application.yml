server:
  port: 8082
  servlet:
    context-path: /rest/coursesApp/1.0
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/coursesappsecured
    username: testing_sec
    password: testing_sec
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
jwt:
  token:
    secret: secretUnknownPasswordYoullNeverGuessInYourEntireExistence
